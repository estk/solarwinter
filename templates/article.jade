
extends layout

block prepend title
  | #{ page.title } - 

block content
  include author
  a.back(href='/') << back to index
  h1= page.title
  p
    | written by:
    mixin author(page.metadata.author)
    p= page.metadata.tags
  div#container
    article.post
      section.content!= page.html

